export const environment = {
  production: true,
  // For production/pilot, set this to your deployed backend URL.
  // When using reverse proxy (same origin), '/api' works directly.
  apiUrl: '/api'
};
